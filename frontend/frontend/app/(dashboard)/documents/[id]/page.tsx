import Link from "next/link";
import { ArrowRight, Download, FileText, Sparkles } from "lucide-react";
import { documentService } from "@/lib/documentService";
export default async function DocumentPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const document = await documentService.get(id); return <div><Link className="back-link" href="/documents"><ArrowRight size={16} /> العودة للمستندات</Link><div className="document-detail"><div className="detail-icon" style={{ background: document.color }}><FileText size={34} /></div><div><p className="eyebrow">{document.type} · {document.size}</p><h1>{document.title}</h1><p className="subheading">آخر تحديث {document.updatedAt}</p></div><button className="outline-button"><Download size={17} /> تحميل</button></div><div className="detail-tabs"><span className="active">الملخص</span><span>المحادثة</span><span>البطاقات</span></div><div className="summary-panel"><div className="summary-header"><span><Sparkles size={18} /> رفيق لخص لك المستند</span><small>محدث الآن</small></div><h2>أهم الأفكار</h2><p>يتناول هذا المستند المفاهيم الأساسية التي تساعدك على فهم الموضوع بشكل عملي. يمكنك مراجعة النقاط الرئيسية ثم اختبار فهمك من خلال البطاقات والاختبارات التفاعلية.</p><ul><li>المفاهيم الأساسية والتعريفات المهمة.</li><li>أمثلة عملية وتطبيقات من العالم الحقيقي.</li><li>خطوات مقترحة للتعمق في الموضوع.</li></ul></div></div>; }